lynx:
  # Elasticsearch plugin configuration
  elasticsearch:
    addresses:
      - "http://localhost:9200"
      - "http://localhost:9201"
    username: "elastic"
    password: "changeme"
    api_key: ""  # Optional: API Key authentication
    service_token: ""  # Optional: Service token authentication
    certificate_fingerprint: ""  # Optional: Certificate fingerprint
    compress_request_body: true
    connect_timeout: "30s"
    max_retries: 3
    enable_metrics: true
    enable_health_check: true
    health_check_interval: "30s"
    index_prefix: "myapp"
    log_level: "info"